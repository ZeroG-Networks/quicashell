# quicashell

## Dependencies

The code builds with:
- zig 0.16.0 on Ubuntu 22.04.5 LTS

## Usage

Run the main program:

    zig run src/quicashell.zig

Indicate a specific local IP address and UDP port to be used:

    TODO

Create a client Initial packet and hex dump it:

    sendinit
